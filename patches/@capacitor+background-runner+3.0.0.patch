diff --git a/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/KV.kt b/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/KV.kt
index 9dfaf18..37ca01d 100644
--- a/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/KV.kt
+++ b/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/KV.kt
@@ -5,6 +5,12 @@ import android.content.SharedPreferences
 import io.ionic.android_js_engine.capacitor_api.KVAPI
 
 class KV (context: android.content.Context, label: String) : KVAPI {
+    companion object {
+        private const val SYNC_LOCK_KEY = "gossip-sync-lock-time"
+        private const val SYNC_LOCK_TIMEOUT_MS = 90_000L
+        private val syncLockMonitor = Any()
+    }
+
     private val prefs: SharedPreferences
 
     init {
@@ -12,9 +18,31 @@ class KV (context: android.content.Context, label: String) : KVAPI {
     }
 
     override fun set(key: String, value: String) {
-        val editor: SharedPreferences.Editor = prefs.edit()
-        editor.putString(key, value)
-        editor.apply()
+        if (key == SYNC_LOCK_KEY) {
+            synchronized(syncLockMonitor) {
+                val existing = prefs.getString(key, null)
+                if (value.startsWith("release:")) {
+                    if (existing == value.removePrefix("release:")) {
+                        prefs.edit().remove(key).commit()
+                    }
+                    return
+                }
+                val existingOwner = existing?.substringAfter(':', "")
+                val requestedOwner = value.substringAfter(':', "")
+                if (!existingOwner.isNullOrEmpty() && existingOwner == requestedOwner) {
+                    prefs.edit().putString(key, value).commit()
+                    return
+                }
+                val existingTime = existing?.substringBefore(':')?.toLongOrNull()
+                val age = existingTime?.let { System.currentTimeMillis() - it }
+                if (age != null && age >= 0 && age < SYNC_LOCK_TIMEOUT_MS) {
+                    return
+                }
+                prefs.edit().putString(key, value).commit()
+            }
+            return
+        }
+        prefs.edit().putString(key, value).apply()
     }
 
     override fun get(key: String): String? {
@@ -22,8 +50,12 @@ class KV (context: android.content.Context, label: String) : KVAPI {
     }
 
     override fun remove(key: String) {
-        val editor: SharedPreferences.Editor = prefs.edit()
-        editor.remove(key)
-        editor.apply()
+        if (key == SYNC_LOCK_KEY) {
+            synchronized(syncLockMonitor) {
+                prefs.edit().remove(key).commit()
+            }
+            return
+        }
+        prefs.edit().remove(key).apply()
     }
 }
\ No newline at end of file
diff --git a/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/Notification.kt b/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/Notification.kt
index 406e572..81ac345 100644
--- a/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/Notification.kt
+++ b/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/Notification.kt
@@ -25,6 +25,7 @@ class Notification(jsonObject: JSONObject) {
     var group: String? = null
     var groupSummary: String? = null
     var channelId: String? = null
+    var gossipOutputGeneration: String
 
     private val jsDateFormat = "yyyy-MM-dd'T'HH:mm:ss.SSS'Z'";
 
@@ -36,6 +37,7 @@ class Notification(jsonObject: JSONObject) {
         ongoing = jsonObject.optBoolean("ongoing", false)
         autoCancel = jsonObject.optBoolean("autoCancel", false)
         actionTypeId = jsonObject.optString("actionTypeId", null)
+        gossipOutputGeneration = jsonObject.optString("gossipOutputGeneration", "0")
 
         val scheduleDateString = jsonObject.optString("scheduleAt", "")
         if (scheduleDateString.isNotEmpty()) {
diff --git a/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/Notifications.kt b/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/Notifications.kt
index 9eb28f3..bcb49c2 100644
--- a/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/Notifications.kt
+++ b/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/Notifications.kt
@@ -202,6 +202,10 @@ class Notifications(context: Context) : NotificationsAPI {
         val intent = Intent(context, TimedNotificationPublisher::class.java)
         intent.putExtra(notificationIntentKey, request.id)
         intent.putExtra(TimedNotificationPublisher.NOTIFICATION_KEY, notification)
+        intent.putExtra(
+            TimedNotificationPublisher.OUTPUT_GENERATION_EXTRA,
+            request.gossipOutputGeneration
+        )
         var flags = PendingIntent.FLAG_CANCEL_CURRENT
         if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.S) {
             flags = flags or PendingIntent.FLAG_MUTABLE
@@ -209,6 +213,14 @@ class Notifications(context: Context) : NotificationsAPI {
 
         val pendingIntent =
             PendingIntent.getBroadcast(context, request.id, intent, flags)
+        val runnerStorage = context.getSharedPreferences(
+            "net.massa.gossip.background.sync",
+            Context.MODE_PRIVATE
+        )
+        val pendingIds = runnerStorage.getStringSet("gossip-runner-notification-ids", emptySet())
+            ?.toMutableSet() ?: mutableSetOf()
+        pendingIds.add(request.id.toString())
+        runnerStorage.edit().putStringSet("gossip-runner-notification-ids", pendingIds).commit()
 
         if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.S && !alarmManager.canScheduleExactAlarms()) {
             alarmManager.setAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, request.scheduleAt.time, pendingIntent)
diff --git a/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/TimedNotificationPublisher.kt b/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/TimedNotificationPublisher.kt
index 7fc1ae3..b73d167 100644
--- a/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/TimedNotificationPublisher.kt
+++ b/node_modules/@capacitor/background-runner/android/src/main/java/io/ionic/backgroundrunner/plugin/api/TimedNotificationPublisher.kt
@@ -10,28 +10,54 @@ import com.getcapacitor.Logger
 class TimedNotificationPublisher : BroadcastReceiver() {
     companion object {
         const val NOTIFICATION_KEY = "NotificationPublisher.notification"
+        const val OUTPUT_GENERATION_KEY = "gossip-account-output-generation-v1"
+        const val OUTPUT_GENERATION_EXTRA = "gossip.accountOutputGeneration"
     }
 
     override fun onReceive(context: Context?, intent: Intent?) {
-        val notificationManager =
-            context!!.getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
+        val appContext = context ?: return
+        synchronized("gossip-account-output-monitor-v1".intern()) {
+            val runnerStorage = appContext.getSharedPreferences(
+                "net.massa.gossip.background.sync",
+                Context.MODE_PRIVATE
+            )
+            val notificationId = intent?.getIntExtra(
+                Notifications.notificationIntentKey,
+                Int.MIN_VALUE
+            ) ?: Int.MIN_VALUE
+            val pendingIds = runnerStorage
+                .getStringSet("gossip-runner-notification-ids", emptySet())
+                ?.toMutableSet() ?: mutableSetOf()
+            pendingIds.remove(notificationId.toString())
+            runnerStorage.edit()
+                .putStringSet("gossip-runner-notification-ids", pendingIds)
+                .commit()
 
-        val notification: android.app.Notification?
+            val blocked = runnerStorage.getString("gossip-account-cleanup-blocked-v1", null)
+            val currentGeneration = runnerStorage.getString(OUTPUT_GENERATION_KEY, "0")
+            val scheduledGeneration = intent?.getStringExtra(OUTPUT_GENERATION_EXTRA)
+            if (!blocked.isNullOrEmpty() || scheduledGeneration != currentGeneration) return
 
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
-            notification = intent!!.getParcelableExtra(NOTIFICATION_KEY, android.app.Notification::class.java)
-        } else {
-            notification = getParcelableExtraLegacy(intent!!)
-        }
+            val notificationManager =
+                appContext.getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
+
+            val notification: android.app.Notification?
+
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
+                notification = intent!!.getParcelableExtra(NOTIFICATION_KEY, android.app.Notification::class.java)
+            } else {
+                notification = getParcelableExtraLegacy(intent!!)
+            }
 
-        notification?.`when` = System.currentTimeMillis()
+            notification?.`when` = System.currentTimeMillis()
 
-        val id =
-            intent.getIntExtra(Notifications.notificationIntentKey, Int.MIN_VALUE)
-        if (id == Int.MIN_VALUE) {
-            Logger.error(Logger.tags("LN"), "No valid id supplied", null)
+            val id =
+                intent.getIntExtra(Notifications.notificationIntentKey, Int.MIN_VALUE)
+            if (id == Int.MIN_VALUE) {
+                Logger.error(Logger.tags("LN"), "No valid id supplied", null)
+            }
+            notificationManager.notify(id, notification)
         }
-        notificationManager.notify(id, notification)
     }
 
     @Suppress("DEPRECATION")
diff --git a/node_modules/@capacitor/background-runner/ios/Sources/CapacitorBackgroundRunner/CapacitorAPI/KV.swift b/node_modules/@capacitor/background-runner/ios/Sources/CapacitorBackgroundRunner/CapacitorAPI/KV.swift
index a9a2e6d..9fce709 100644
--- a/node_modules/@capacitor/background-runner/ios/Sources/CapacitorBackgroundRunner/CapacitorAPI/KV.swift
+++ b/node_modules/@capacitor/background-runner/ios/Sources/CapacitorBackgroundRunner/CapacitorAPI/KV.swift
@@ -8,7 +8,42 @@ import JavaScriptCore
 }
 
 class CapacitorKVStore: NSObject, CapacitorKVStoreExports {
+    private static let syncLockKey = "gossip-sync-lock-time"
+    private static let syncLockTimeoutMs: Int64 = 90_000
+    private static let syncLockMonitor = NSLock()
+
     static func set(_ key: String, _ value: String) {
+        if key == syncLockKey {
+            syncLockMonitor.lock()
+            defer { syncLockMonitor.unlock() }
+            if value.hasPrefix("release:") {
+                let owner = String(value.dropFirst("release:".count))
+                if UserDefaults.standard.string(forKey: key) == owner {
+                    UserDefaults.standard.removeObject(forKey: key)
+                    _ = UserDefaults.standard.synchronize()
+                }
+                return
+            }
+            if let existing = UserDefaults.standard.string(forKey: key) {
+                let existingOwner = existing.split(separator: ":", maxSplits: 1).last.map(String.init)
+                let requestedOwner = value.split(separator: ":", maxSplits: 1).last.map(String.init)
+                if existingOwner == requestedOwner {
+                    UserDefaults.standard.set(value, forKey: key)
+                    _ = UserDefaults.standard.synchronize()
+                    return
+                }
+            }
+            if let existing = UserDefaults.standard.string(forKey: key),
+               let time = Int64(existing.split(separator: ":", maxSplits: 1)[0]) {
+                let age = Int64(Date().timeIntervalSince1970 * 1_000) - time
+                if age >= 0 && age < syncLockTimeoutMs {
+                    return
+                }
+            }
+            UserDefaults.standard.set(value, forKey: key)
+            _ = UserDefaults.standard.synchronize()
+            return
+        }
         UserDefaults.standard.set(value, forKey: key)
     }
 
@@ -24,6 +59,13 @@ class CapacitorKVStore: NSObject, CapacitorKVStoreExports {
     }
 
     static func remove(_ key: String) {
-        return UserDefaults.standard.removeObject(forKey: key)
+        if key == syncLockKey {
+            syncLockMonitor.lock()
+            defer { syncLockMonitor.unlock() }
+            UserDefaults.standard.removeObject(forKey: key)
+            _ = UserDefaults.standard.synchronize()
+            return
+        }
+        UserDefaults.standard.removeObject(forKey: key)
     }
 }
diff --git a/node_modules/@capacitor/background-runner/ios/Sources/CapacitorBackgroundRunner/CapacitorAPI/Notifications.swift b/node_modules/@capacitor/background-runner/ios/Sources/CapacitorBackgroundRunner/CapacitorAPI/Notifications.swift
index 1e33243..01854a5 100644
--- a/node_modules/@capacitor/background-runner/ios/Sources/CapacitorBackgroundRunner/CapacitorAPI/Notifications.swift
+++ b/node_modules/@capacitor/background-runner/ios/Sources/CapacitorBackgroundRunner/CapacitorAPI/Notifications.swift
@@ -32,6 +32,7 @@ struct NotificationOption {
     let threadIdentifier: String?
     let summaryArgument: String?
     let groupSummary: String?
+    let gossipOutputGeneration: String
 
     init(from dict: [String: Any?]) throws {
         guard let id = dict["id"] as? Int else {
@@ -60,6 +61,7 @@ struct NotificationOption {
         self.extra = dict["extra"] as? [String: Any]
         self.summaryArgument = dict["summaryArgument"] as? String
         self.groupSummary = dict["groupSummary"] as? String
+        self.gossipOutputGeneration = dict["gossipOutputGeneration"] as? String ?? "0"
     }
 }
 
@@ -131,8 +133,18 @@ class CapacitorNotifications: NSObject, CapacitorNotificationsExports {
             }
 
             if let notificationOptions = options.toArray() as? [[String: Any?]] {
+                let scheduling = DispatchGroup()
                 for option in notificationOptions {
                     let notificationOption = try NotificationOption(from: option)
+                    let blocked = UserDefaults.standard.string(
+                        forKey: "gossip-account-cleanup-blocked-v1"
+                    )
+                    let currentGeneration = UserDefaults.standard.string(
+                        forKey: "gossip-account-output-generation-v1"
+                    ) ?? "0"
+                    if blocked != nil || notificationOption.gossipOutputGeneration != currentGeneration {
+                        continue
+                    }
 
                     let content = UNMutableNotificationContent()
                     content.title = NSString.localizedUserNotificationString(forKey: notificationOption.title, arguments: nil)
@@ -172,8 +184,14 @@ class CapacitorNotifications: NSObject, CapacitorNotificationsExports {
                     let trigger = UNTimeIntervalNotificationTrigger(timeInterval: dateInterval.duration, repeats: false)
                     let request = UNNotificationRequest(identifier: "\(notificationOption.id)", content: content, trigger: trigger)
 
-                    UNUserNotificationCenter.current().add(request)
+                    scheduling.enter()
+                    UNUserNotificationCenter.current().add(request) { _ in
+                        scheduling.leave()
+                    }
                 }
+                // Keep the runner's owner lock until every request is accepted;
+                // import fencing waits that lock before removing pending requests.
+                scheduling.wait()
             }
         } catch {
             JSContext.current().exception = JSValue(newErrorFromMessage: "\(error)", in: JSContext.current())
