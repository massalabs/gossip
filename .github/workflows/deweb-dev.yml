name: CD - Deploy Dev to DeWeb

on:
  push:
    branches: [dev]

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install
        run: npm ci

      - name: Get commit hash
        id: vars
        run: echo "commit_hash=$(git rev-parse --short=7 HEAD)" >> $GITHUB_OUTPUT

      - name: Build
        env:
          VITE_PROTOCOL_API_URL: ${{ secrets.VITE_PROTOCOL_API_URL }}
          VITE_APP_BUILD_ID: ${{ steps.vars.outputs.commit_hash }}
        run: npm run build

      - name: Deploy Dev → buildnet
        id: deploy
        uses: massalabs/deweb-gh-action@v0.1.0
        with:
          config_file: ./deweb_cli_config.dev.buildnet.json
          source_folder: ./dist
        env:
          MASSA_DEWEB_SECRET_KEY: ${{ secrets.PRIVATE_KEY || secrets.MASSA_DEWEB_MAINNET_PKEY }}

      - name: URL
        run: echo "Dev deployed → ${{ steps.deploy.outputs.deployed-website }}"
